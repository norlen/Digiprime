<% layout('layouts/boilerplate') %>

<div class="container mt-3">
  <div class="row">
    <!-- Vänster kolumn - Offers -->
    <div class="col-sm-6">
      <div class="col" id="eachBlock">
        <div class="card mb-3">
          <img
            class="img-fluid"
            alt=""
            src="https://res.cloudinary.com/diq0t2bqj/image/upload/v1622925764/YelpCamp/kjhxxshjrdudgkoehoyl.jpg"
          />

          <div class="accordion accordion-flush" id="accordionTest">
            <div class="accordion-item">
              <h2 class="accordion-header" id="headingOne">
                <button
                  class="accordion-button collapsed fs-4 fw-bold"
                  type="button"
                  data-bs-toggle="collapse"
                  data-bs-target="#collapseOne"
                  aria-expanded="false"
                  aria-controls="collapseOne"
                >
                  <%= offer.title %> <span class="text-white">.</span>
                  <span class="badge rounded-pill bg-success"
                    ><%= offer.costumer %></span
                  >
                </button>
              </h2>
              <div
                id="collapseOne"
                class="accordion-collapse collapse"
                aria-labelledby="headingOne"
                data-bs-parent="#accordionTest"
              >
                <div class="accordion-body">
                  <div class="list-group list-group-flush">
                    <li class="list-group-item">
                      <%= offer.referenceSector %>
                    </li>
                    <li class="list-group-item"><%= offer.referenceType %></li>
                    <li class="list-group-item">$<%= offer.price %> / Piece</li>
                    <li class="list-group-item"><%= offer.description %></li>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <ul class="list-group list-group-flush">
            <li class="list-group-item text-muted"><%= offer.location %></li>
            <li class="list-group-item">
              Submitted by <%= auction.payload.created_by.val[0] %>
            </li>
          </ul>

          <div class="card-footer text-muted">
            created <%= formatDistanceToNow(new
            Date(auction.payload.created_at.val[0])) %> ago
          </div>
        </div>
      </div>

      <div class="mb-5">
        <a class="card-link btn btn-primary" href="/auctions"
          ><svg
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            fill="currentColor"
            class="bi bi-arrow-bar-left"
            viewBox="0 0 16 16"
          >
            <path
              fill-rule="evenodd"
              d="M12.5 15a.5.5 0 0 1-.5-.5v-13a.5.5 0 0 1 1 0v13a.5.5 0 0 1-.5.5zM10 8a.5.5 0 0 1-.5.5H3.707l2.147 2.146a.5.5 0 0 1-.708.708l-3-3a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L3.707 7.5H9.5a.5.5 0 0 1 .5.5z"
            />
          </svg>
          <span>All auctions</span></a
        >
      </div>
    </div>

    <!-- Höger kolumn -->
    <div class="col-sm-6">
      <!-- Info om rum och lägga bud -->
      <div class="card">
        <div class="card-body bg-light text-center">
          <h3>
            Auction: <%= auction.payload.room_name.val[0] %>
            <!-- prettier-ignore -->
            <% if (auction.ended) { %>
            <span class="badge bg-danger">ENDED</span>
            <% } else if (auction.closed) { %>
            <span class="badge bg-danger">CLOSED</span>
            <% } else { %>
            <span class="badge bg-primary">
              <%= auction.payload.auction_type.val[0] %>
            </span>
            <% } %>
          </h3>
        </div>

        <div class="card-body text">
          <table class="table table-borderless">
            <tbody>
              <td>Starting bid:</td>
              <td class="text-end">$<%= offer.price %></td>
            </tbody>
            <tbody>
              <td>Closing time:</td>
              <td class="text-end">
                <%= displayDate(auction.payload.closing_time.val[0]) %>
              </td>
            </tbody>
          </table>

          <% var auctioneer = auction.members[0].added_by ==
          currentUser.username ? 1 : 0; %>
          <!-- 0 = budare, 1 = skapare av auktion -->
          <% if (auctioneer && auction.closed && !auction.ended) { %> <% if
          (auction.bids.length == 0) { %>
          <button type="button" class="card-link btn btn-danger" disabled>
            Select Winner
          </button>
          <small class="text-muted d-block mt-1"
            >Cannot select winner. No bids have been placed.</small
          >
          <% } else { %>
          <button
            type="button"
            class="card-link btn btn-info col-12"
            data-bs-toggle="modal"
            data-bs-target="#pickWinnerModal"
          >
            Pick Winner
          </button>
          <% } %> <% } else if (!auctioneer && !auction.ended &&
          !auction.closed) { %>

          <label class="form-label" for="bid">Offer Price</label>
          <form class="" method="POST" action="/auctions/<%= auction._id %>">
            <div class="input-group mb-3">
              <span class="input-group-text" id="bid-label">$</span>
              <input
                type="text"
                class="form-control"
                id="bid"
                placeholder="0.00"
                aria-label="bid"
                aria-describedby="bid-label"
                name="bid"
                required
              />
            </div>
            <button type="submit" class="card-link btn btn-info col-12">
              Place Bid
            </button>
          </form>

          <% } %>
        </div>
      </div>

      <br />
      <h3>Bid History</h3>

      <!-- Bud historik-->
      <div class="card">
        <table class="table">
          <% for (let bid of auction.bids) { %>
          <tbody>
            <td><%= bid.sender %></td>
            <td><%= displayDate(bid.created_at) %></td>
            <td>$<%= bid.text %></td>
          </tbody>

          <% } %>
        </table>

        <a class="btn btn-primary" href="/auctions/<%= auction._id %>/bids">
          View all bids</a
        >
      </div>
    </div>
  </div>

  <div
    class="modal fade"
    id="pickWinnerModal"
    tabindex="-1"
    aria-labelledby="exampleModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog modal-lg">
      <form class="" method="POST" action="/auctions/<%= auction._id %>/end">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">
              Select Winning Bid
            </h5>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <div class="table-responsive-sm">
              <table class="table table-hover">
                <thead>
                  <tr>
                    <th scope="col">Sender</th>
                    <th scope="col">Distance</th>
                    <th scope="col">Bid</th>
                    <th scope="col">Placed</th>
                    <th scope="col">Select as winner</th>
                  </tr>
                </thead>
                <tbody>
                  <% for (let bid of auction.bids) { %>
                  <tr>
                    <th scope="row"><%= bid.sender %></th>
                    <td><%= Math.round(bid.distance) %></td>
                    <td><%= bid.text %></td>
                    <td><%= displayDate(bid.created_at) %></td>
                    <td class="d-flex justify-content-center">
                      <input
                        type="radio"
                        class="btn-check"
                        name="winner"
                        id="<%= bid.sender %>"
                        autocomplete="off"
                        value="<%= bid.sender %>"
                      />
                      <label
                        class="btn btn-outline-primary"
                        for="<%= bid.sender %>"
                        >Select</label
                      >
                    </td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
          <div class="modal-footer">
            <button
              type="button"
              class="btn btn-secondary"
              data-bs-dismiss="modal"
            >
              Close
            </button>
            <button type="submit" class="btn btn-primary">End auction</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
